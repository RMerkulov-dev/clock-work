import React from "react";
import { Modal } from "../index";

const RegisterModal = () => {
  return (
    <>
      <Modal onClose={() => {}} onSubmit={() => {}} actionLabel="Modal" />
    </>
  );
};

export default RegisterModal;
